#!/bin/bash
# ════════════════════════════════════════════════════════════════════════
# AWS DEPLOYMENT - STEP BY STEP SSH COMMANDS
# ════════════════════════════════════════════════════════════════════════
# 
# Run these commands ONE BY ONE in your AWS SSH terminal
# Copy and paste each command, wait for it to complete, then move to next
#
# Server: 13.218.100.97
# User: ubuntu
# Folder: /home/ubuntu/puppeteer-server
#
# ════════════════════════════════════════════════════════════════════════

# STEP 1: SSH into AWS
# ────────────────────────────────────────────────────────────────────────
# Copy this entire command and paste in your local terminal:

ssh ubuntu@13.218.100.97

# You should now be in AWS. Continue with remaining steps in that SSH session.
# ════════════════════════════════════════════════════════════════════════


# STEP 2: Check if Node.js is installed
# ────────────────────────────────────────────────────────────────────────
# Run this in SSH:

node --version

# If you see v18+ or higher, you can skip to STEP 5
# If "command not found", continue with STEP 3


# STEP 3: Install Node.js v20 LTS (if not installed)
# ────────────────────────────────────────────────────────────────────────
# Run these commands one by one:

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -

sudo apt-get install -y nodejs

node --version

# Should show v20.x.x
# ════════════════════════════════════════════════════════════════════════


# STEP 4: Check if Chrome/Chromium is installed
# ────────────────────────────────────────────────────────────────────────
# Run this:

chromium-browser --version

# If "command not found", continue to install it


# STEP 4b: Install Chromium (if not installed)
# ────────────────────────────────────────────────────────────────────────
# Run this:

sudo apt-get update

sudo apt-get install -y chromium-browser

chromium-browser --version

# Should show version number
# ════════════════════════════════════════════════════════════════════════


# STEP 5: Navigate to server folder
# ────────────────────────────────────────────────────────────────────────
# Run this:

cd /home/ubuntu/puppeteer-server

# Verify you're in the right place:
pwd

# Should show: /home/ubuntu/puppeteer-server
# ════════════════════════════════════════════════════════════════════════


# STEP 6: List current files
# ────────────────────────────────────────────────────────────────────────
# Run this to see what's there:

ls -lh

# You should see server.js or server.cjs
# ════════════════════════════════════════════════════════════════════════


# STEP 7: Stop the old server (if running)
# ────────────────────────────────────────────────────────────────────────
# Run this:

pkill -f "node server"

# Wait 2 seconds for it to stop
sleep 2

# Verify it stopped:
ps aux | grep node

# Should NOT see any "node server" process
# ════════════════════════════════════════════════════════════════════════


# STEP 8: Install npm dependencies
# ────────────────────────────────────────────────────────────────────────
# Run this (production only - smaller, faster):

npm install --production

# Wait for it to complete (2-3 minutes)
# You should see "added XX packages"
# ════════════════════════════════════════════════════════════════════════


# STEP 9: Verify dependencies installed
# ────────────────────────────────────────────────────────────────────────
# Run this:

npm ls --depth=0

# Should show:
# ├── puppeteer-core@24.32.1
# ├── puppeteer-extra@3.3.6
# ├── puppeteer-extra-plugin-stealth@2.11.2
# ├── user-agents@1.1.669
# ├── express@5.2.1
# └── axios@1.13.2
# ════════════════════════════════════════════════════════════════════════


# STEP 10: Check node_modules size
# ────────────────────────────────────────────────────────────────────────
# Run this:

du -sh node_modules

# Should be around 100-200MB
# ════════════════════════════════════════════════════════════════════════


# STEP 11: Start the server in background
# ────────────────────────────────────────────────────────────────────────
# Run this:

nohup node server.cjs > puppeteer.log 2>&1 &

# Wait 2 seconds for it to start
sleep 2

# Verify it started:
ps aux | grep "node server"

# Should see the process running
# ════════════════════════════════════════════════════════════════════════


# STEP 12: Check server logs
# ────────────────────────────────────────────────────────────────────────
# Run this:

tail -20 puppeteer.log

# Should show:
# 🚀 Puppeteer Real-Device Mode with SERP + Luna Proxy
# ✅ Server running on port 3000 - Real-Device Mode Active
# ════════════════════════════════════════════════════════════════════════


# STEP 13: Test the health endpoint
# ────────────────────────────────────────────────────────────────────────
# Run this:

curl http://localhost:3000/health

# Should respond with:
# {"status":"ok","features":["Real-Device Mode","SERP API","Luna Proxy","Fingerprinting"]}
# ════════════════════════════════════════════════════════════════════════


# STEP 14: Verify from outside (from your local machine)
# ────────────────────────────────────────────────────────────────────────
# Open a NEW terminal on your local machine (don't close SSH):

curl http://13.218.100.97:3000/health

# Should return the same JSON response
# ════════════════════════════════════════════════════════════════════════


# STEP 15: Monitor the server (back in SSH)
# ────────────────────────────────────────────────────────────────────────
# Run this to watch logs in real-time:

tail -f puppeteer.log

# Press Ctrl+C to stop watching
# ════════════════════════════════════════════════════════════════════════


# TROUBLESHOOTING COMMANDS
# ════════════════════════════════════════════════════════════════════════

# Check if port 3000 is listening:
netstat -tulpn | grep 3000

# Kill server if needed:
pkill -f "node server"

# Restart server:
nohup node server.cjs > puppeteer.log 2>&1 &

# View full logs:
cat puppeteer.log

# Check system resources:
free -h

# Check disk space:
df -h

# Check running processes:
ps aux | grep -E "node|chromium"
